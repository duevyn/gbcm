# Gameboy Color Emulator (gbcm)

## Download

```
# You need to clone with submodules, otherwise SDL will not download.
git clone https://github.com/duevyn/gbcm --depth=1 --recurse-submodules
```

## Run

```
# build
cmake -S . -B build
cmake --build build

# run
./build/gbcm
```
